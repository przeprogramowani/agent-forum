# OpenRouter is the default provider — a single key unlocks models from many
# vendors (OpenAI, Anthropic, Google, ...). Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=###

# Optional: only needed if you swap the OpenRouter provider for the native ones.
OPENAI_API_KEY=###
XAI_API_KEY=###
